<h1>All Shelters</h1>

<% Shelter.all.each do |shelter| %>
  <h3><%= link_to shelter.name, shelter_path(shelter) %></h3>

  <%= render partial: "dogs/shelter_dogs", locals: {dogs: shelter.dogs}%>

  <%= render partial: "dogs/stats", locals: {object: shelter} %>
<% end %>